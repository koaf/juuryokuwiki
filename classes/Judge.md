# Judge クラス

## 📋 基本情報

| 項目 | 内容 |
|------|------|
| **ファイル** | `Judge.java` |
| **役割** | 審判・盤面管理・勝敗判定 |
| **作者** | k_maeno |

## 📝 説明

審判クラス。盤面の状態を管理し、石の配置可否判定、勝敗判定を行います。

## 🔧 フィールド

| 修飾子 | 型 | 名前 | 説明 |
|--------|------|------|------|
| `private final` | `int` | `TO_WIN` | 勝つために必要な石の数 |
| `private` | `int[][]` | `grid` | 盤面の情報保存用配列 |

### 盤面データの形式

```java
grid[行][列]
// -1: 置いていない（空）
//  0: 先攻の石
//  1: 後攻の石
```

## 🔧 コンストラクタ

### Judge(int row, int col, int toWin)

```java
public Judge(int row, int col, int toWin)
```

**パラメータ:**
- `row` - 盤面の行数
- `col` - 盤面の列数
- `toWin` - 勝利に必要な連続石数

## 🔧 メソッド

### canPut(int col)

```java
public int canPut(int col)
```

コマが置けるかどうか判定します（置き場所判定）。

**パラメータ:**
- `col` - 指定列

**戻り値:**
- 置ける場合: `col`（指定列番号）
- 置けない場合: `-1`

**注意:** 返り値は「置ける行番号」ではなく「指定列番号」

### put(int order, int col)

```java
public int put(int order, int col)
```

コマを置きます（置き場所判定済みの前提）。

**パラメータ:**
- `order` - 先攻後攻（0:先攻, 1:後攻）
- `col` - 指定列

**戻り値:**
- 成功: 置けた行番号（0～row-1）
- 失敗: `-1`

### checkWinner(int order, int row, int col)

```java
public boolean checkWinner(int order, int row, int col)
```

指定された石が四(n)目そろったか判定します（勝負判定）。

**パラメータ:**
- `order` - 先攻後攻（0:先攻, 1:後攻）
- `row` - 置いた石の行番号
- `col` - 置いた石の列番号

**戻り値:**
- `true`: 勝敗決定
- `false`: 勝敗つかず

### checkLine(int row, int col, int order, int count, int moveX, int moveY, boolean isPositive)

```java
public boolean checkLine(int row, int col, int order, int count, int moveX, int moveY, boolean isPositive)
```

1方向の並びを再帰処理にて確認。移動量を変えることで縦横斜めに全対応。

**パラメータ:**
- `row` - チェックする行番号
- `col` - チェックする列番号
- `order` - 先手・後手
- `count` - 並んだ石の数（1からスタート）
- `moveX` - x方向(col)の移動量
- `moveY` - y方向(row)の移動量
- `isPositive` - 進行方向（falseならマイナス方向へ移動）

**戻り値:**
- `true`: 並んだ
- `false`: 並んでいない

### showGrid()

```java
public void showGrid()
```

マス目領域の状態を表示します（デバッグ用）。

**出力例:**
```
-------
-------
-------
-------
○×-----
○○×----
```

### initGrid()

```java
public void initGrid()
```

マス目領域を初期化します。

### getGrid()

```java
public int[][] getGrid()
```

現在の盤面の情報を他のクラスから参照するときに使用。

**戻り値:**
- `grid` - 現在の盤面データ

## 📄 勝利判定のアルゴリズム

```java
public boolean checkWinner(int order, int row, int col) {
    // 横、縦、斜めの4つの連続を探す
    // どれか1つでもカウントがTO_WINになれば決着
    if (checkLine(row, col, order, 1, 1, 0, true) ||   // 横
        checkLine(row, col, order, 1, 0, 1, true) ||   // 縦
        checkLine(row, col, order, 1, 1, 1, true) ||   // 斜め ＼
        checkLine(row, col, order, 1, 1, -1, true)) {  // 斜め ／
        return true;
    }
    return false;
}
```

### checkLine の動作

```
checkLine の探索パターン

横方向 (moveX=1, moveY=0):
  ← ← ← [石] → → →

縦方向 (moveX=0, moveY=1):
     ↑
     ↑
    [石]
     ↓
     ↓

斜め＼ (moveX=1, moveY=1):
  ↖       
    ↖     
     [石]   
       ↘   
         ↘

斜め／ (moveX=1, moveY=-1):
         ↗
       ↗
     [石]
    ↙
  ↙
```

## 🔗 関連クラス

- [Game](Game.md) - ゲーム進行管理
- [Com](Com.md) - Comから `getGrid()` で盤面を取得

## 💡 使用例

```java
// AIでの使用例（Com.java内）
int[][] grid = judge.getGrid();  // 盤面データを取得

// 合法手の確認
for (int col = 0; col < 7; col++) {
    if (judge.canPut(col) != -1) {
        // この列には置ける
    }
}
```

---

[← Board](Board.md) | [Player →](Player.md)
