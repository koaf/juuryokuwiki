# Man クラス

## 📋 基本情報

| 項目 | 内容 |
|------|------|
| **ファイル** | `Man.java` |
| **役割** | 人間プレイヤーの実装 |
| **継承** | `Player` |
| **作者** | k_maeno |

## 📝 説明

重力四目並べの人間（プレイヤー）を表すクラス。`Player`クラスを継承して実装されています。

## 🔧 フィールド

| 修飾子 | 型 | 名前 | 説明 |
|--------|------|------|------|
| `private` | `Board` | `board` | 盤面情報（ボタン入力の取得に使用） |

## 🔧 コンストラクタ

### Man(String name, int order, Board board, String message, String imageURI)

```java
public Man(String name, int order, Board board, String message, String imageURI)
```

**パラメータ:**
- `name` - プレイヤーの名前
- `order` - 先攻後攻
- `board` - 盤面情報
- `message` - プレイヤーの情報
- `imageURI` - プレイヤーの画像パス

### Man(int order, Board board)

```java
public Man(int order, Board board)
```

簡易コンストラクタ。デフォルト値を使用します。

**パラメータ:**
- `order` - 先攻後攻
- `board` - 盤面情報

**デフォルト値:**
- 名前: `"人間"`
- 情報: `"全力で頑張ります"`
- 画像: `"image/player_128x128.jpg"`

## 🔧 メソッド

### put(Judge judge)

```java
@Override
public int put(Judge judge) throws IOException
```

人間（プレイヤー）がコマを盤のどこに置くか、ボタン入力を待ちます。

**パラメータ:**
- `judge` - 審判クラス（盤面情報の取得に使用可能）

**戻り値:**
- 列番号（0～6）

**処理内容:**
1. 無限ループでボタンクリックを待つ
2. `board.getPushButtonNo()` で押されたボタン番号を取得
3. ボタンが押されたら（-1以外）ループを抜ける
4. ボタン番号をリセットして列番号を返す

## 📄 ソースコード

```java
public class Man extends Player {
    Board board;

    public Man(String name, int order, Board board, String message, String imageURI) {
        super(name, order, message, imageURI);
        this.board = board;
    }

    public Man(int order, Board board) {
        this(
            "人間",  // TODO: プレイヤー名を定義
            order,
            board,
            "全力で頑張ります",  // TODO: プレイヤー情報を記載
            "image/player_128x128.jpg"  // TODO: 画像パスを指定
        );
    }

    @Override
    public int put(Judge judge) throws IOException {
        int[][] grid = judge.getGrid();
        int col = -1;
        while(true) {
            try {
                if((col = board.getPushButtonNo()) != -1) {
                    board.resetPushButtonNo();
                    break;
                }
                Thread.sleep(200);
            } catch (InterruptedException e) {
                System.out.println(e);
            }
        }
        return col;
    }
}
```

## 📊 処理フロー

```
put() 呼び出し
      │
      ▼
┌─────────────────────┐
│ while(true) ループ  │
│                     │
│  ┌───────────────┐  │
│  │ボタンクリック待ち│  │
│  │(200msスリープ)  │  │
│  └───────────────┘  │
│         │           │
│         ▼           │
│  ボタン押された？    │
│    │NO      │YES    │
│    └───┐    │       │
│        │    ▼       │
│        │  break     │
│        └───┘        │
└─────────────────────┘
      │
      ▼
  列番号を返す
```

## 🔗 関連クラス

- [Player](Player.md) - 親クラス
- [Board](Board.md) - ボタン入力の取得に使用
- [Com](Com.md) - 対となるコンピュータプレイヤー

## 💡 カスタマイズ

### プレイヤー情報の変更

`Game.java` の `init()` メソッドで変更：

```java
// カスタム名前・情報・画像
players[order] = new Man(
    "山田太郎",
    order,
    board,
    "必ず勝つ！",
    "image/myplayer.jpg"
);
```

---

[← Player](Player.md) | [Com →](Com.md)
